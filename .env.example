# ── Server ──
PORT=3000
NODE_ENV=development

# ── BREM runtime mode ──
# development | production
BREM_MODE=development
# local | supabase
BREM_BACKEND=local
# true = Supabase 실패 시 localStorage 폴백 (개발 전용)
BREM_ALLOW_LOCAL_FALLBACK=true

# ── Supabase (Dashboard → Project Settings → API) ──
SUPABASE_URL=https://YOUR_PROJECT.supabase.co
SUPABASE_ANON_KEY=YOUR_SUPABASE_ANON_KEY
# 서버 전용 — Vercel에만 설정, Git/프론트 절대 금지
SUPABASE_SERVICE_ROLE_KEY=YOUR_SUPABASE_SERVICE_ROLE_KEY
SUPABASE_FUNCTIONS_URL=https://YOUR_PROJECT.supabase.co/functions/v1

# ── Admin bootstrap (공개 설정에 loginName/email만 노출) ──
BREM_ADMIN_LOGIN_NAME=관리자
BREM_ADMIN_EMAIL=kim01022680704@gmail.com

# ── 배민Biz 자동 수집 (서버 전용, Git/프론트 절대 금지) ──
# Supabase settings(brem_baemin_biz_session)에 저장하는 것을 권장합니다.
# 아래 env는 레거시/비상 fallback 용도입니다.
BAEMIN_BIZ_SESSION_COOKIE=
# 배민Biz 로그인: 비우면 아이디/비번·휴대폰 전부 수동(지금 떠 있는 비즈 로그인 화면 유지)
# 채우면 아이디/비번만 자동 입력 — 휴대폰 인증은 항상 수동
BAEMIN_BIZ_LOGIN_ID=
BAEMIN_BIZ_LOGIN_PASSWORD=
# 로컬 Playwright 세션 갱신 + 자동 수집 스케줄 서버 포트 (PC에서만 설정, 기본 3939)
# npm run baemin:session-server 실행 시 KST 10:00, 14:00, 17:00, 20:00, 23:30 자동 수집
# Vercel/운영 서버에는 설정하지 마세요 — ERP는 항상 PC localhost:3939 로 연결합니다.
BAEMIN_SESSION_LOCAL_PORT=3939
# PC에서 baemin:session-server 실행 시 필수 — Vercel 배포 보호 우회용 Supabase 직접 저장
# (위 SUPABASE_URL + SUPABASE_SERVICE_ROLE_KEY 와 동일 프로젝트 값 사용)
# VERCEL_AUTOMATION_BYPASS_SECRET=  # 선택: brem.kr HTTP API fallback 시 Vercel Dashboard → Deployment Protection → Bypass
# 로컬 수집기가 호출할 운영 URL
BREM_PUBLIC_URL=https://brem.kr

# ── 크롤링 원버튼 / 상시기동 (PC 로컬 세션서버 전용) ──
# 탑바 「크롤링 시작」 노출 — 권장: ERP 관리자계정 → 「크롤링 노출」 체크
# 아래 env는 계정 플래그가 아직 없는 경우의 레거시 폴백
# 예: 김형진,admin.g7yfepgm@gmail.com
CRAWL_OPERATOR_ADMIN_IDS=김형진,admin.g7yfepgm@gmail.com
# 쿠팡 파트너 자동로그인 (로그아웃 잦음 → 서버 기동/토큰 소실 시 자동 재로그인)
COUPANG_LOGIN_ID=
COUPANG_LOGIN_PASSWORD=
# 쿠팡 이메일 2FA용 네이버 메일 자동로그인
NAVER_LOGIN_ID=
NAVER_LOGIN_PASSWORD=
# NAVER_PLAYWRIGHT_PROFILE=.naver-playwright-profile
# 세션서버 재시작 시 현황 자동순회 재개
# BAEMIN_AUTO_RESUME_STATUS_LOOP=1
# COUPANG_AUTO_RESUME_STATUS_LOOP=1
COUPANG_SESSION_LOCAL_PORT=3940
# 배민 세션서버 복구: POST http://127.0.0.1:3939/auth/recover
# 쿠팡 세션서버 복구: POST http://127.0.0.1:3940/auth/recover
# 콜수/거절율/라이더앱 반영 확인사살 (KST, 배민 세션서버가 스케줄)
# CRAWL_ERP_PUBLISH_SCHEDULE=1
# CRAWL_ERP_PUBLISH_SLOTS=07:00,11:30,14:00,22:00
# 라이더반영 N분 전 8일 재수집(배민 일별·라이더별 + 쿠팡 라이더). 기본 30분 전
# CRAWL_WEEKLY_REFRESH_SCHEDULE=1
# CRAWL_WEEKLY_REFRESH_OFFSET_MIN=30
# CRAWL_WEEKLY_REFRESH_SLOTS=06:30,11:00,13:30,21:30
